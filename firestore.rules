rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access for application operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
